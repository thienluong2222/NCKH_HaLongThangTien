# ==========================================
# ICHeritage Backend - CPU-only Requirements
# Optimized for smaller Docker image (~3-4GB instead of 13GB)
# ==========================================

# --- PyTorch CPU-only (MUST be installed first) ---
# This installs torch without CUDA, saving ~8GB
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu

# --- Web & API ---
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0
python-dotenv>=1.0.0
gunicorn>=21.0.0

# --- AI & LLM ---
langchain>=0.1.0
langchain-google-genai>=1.0.0
langchain-core>=0.1.0
google-generativeai>=0.3.0

# --- Computer Vision ---
opencv-python-headless>=4.8.0
ultralytics>=8.0.0
pillow>=10.0.0

# --- Data Science ---
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
matplotlib>=3.7.0

# --- Utilities ---
pydantic>=2.0.0
tqdm>=4.65.0
